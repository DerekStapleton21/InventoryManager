@model IEnumerable<Inventory_Manager.Models.UserParts>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Part)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Part.PartName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserPartsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserPartsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserPartsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
